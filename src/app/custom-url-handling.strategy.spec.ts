import { CustomUrlHandling } from './custom-url-handling.strategy';

describe('CustomUrlHandling', () => {
  it('should create an instance', () => {
    expect(new CustomUrlHandling()).toBeTruthy();
  });
});
